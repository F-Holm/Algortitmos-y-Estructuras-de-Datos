Cosas fundamentales:

- Funciones
- Referencias
- Variables primitivas y su tamaño
- void
- punteros
- operador ternario -> condición ? true : false
- memoria dinámicas
- arreglos dinámicos
- string

Librerías y Funciones:

#include <string>
- string -> 16B (en 32bits) -> ptr inicio, ptr final, size reservado, size libre
- str.clear()
- empty
- size
- length
- erase
- insert

#include <iostream>
- cin
- cout
- endl -> cout << endl; == cout << '\n' << flush;
- cin.get(&char)
- cin.ignore() -> vacía el buffer de entrada
- boolalpha -> ahora los booleanos se muestran como true o false
- noboolalpha -> ahora los booleanos se muestran como 0 o 1 (default)

#include <cmath>
- pow(x, y) -> Potencia -> x^y -> double pow(double base, double exponent);
- sqrt(x) -> Raiz cuadrada -> double sqrt(double x);
- abs(x) -> Módulo (entero) -> int abs(int x);
- fabs(x) -> Módulo (punto flotante) -> double fabs(double x);
- sin(x) -> seno radianes | grados a radianes -> x*PI/180 -> double sin(double x);
- cos(x) -> coseno radianes
- atan(x) -> arco tangente radianes
- log(x) -> logaritmo natural
- log10(x) -> log base 10
- exp(x) -> e^x | exp(x * log(y)) = y^x
- ceil(x) -> parte entera para arriba -> ceil(3.1) = 4.0 -> ceil(-3.1) = -3.0
- floor(x) -> parte entera para abajo -> floor(3.1) = 3.0 -> floor(-3.1) = -4.0
- fmod(x, y) -> resto de x/y
- modf(x, y) -> retorna parte entera y parte decimal -> y por referencia | retorna parte decimal -> x, y, y return son doubles
- hypot(x, y) -> pitágoras
- poly(x, n, c) -> Borland -> (x = var. independiente | n = grado polinomio | c = conjunto de coeficientes)

#include <cstdlib>
- div(x, y) -> retorna registro/struct (div_t -> int quot, int rem)
- random(x) -> Borland -> [0; x) = [0; x-1] -> random(MAX - MIN + 1) + MIN
- rand() -> standard -> [0; RAND_MAX] -> rand() % x | [0; x) = [0; x-1]
- randomize() -> Borland
- srand(x) -> standard

#include <ctime>
- time(*p) -> retorna N° sw s desde 1-1-1970 -> Se recomienda nullptr para c++11 o más moderno, NULL para Borland (no existe nullptr)

#include <cctype>
- tolwr(char) -> TO_LOWER
- toupper(char) -> TO_UPPER

#include <cstring>
- strcmp(c_str, c_str) == 0 -> iguales
- strcpy(c_str destino, c_str src)
- strcat(c_str, c_str)
- strlen(c_str)
- strstr(c_str, c_str) -> se fija si existe la cadena 2 en la cadena 1 -> devuelve puntero al inicio de la cadena 2 contenida en la uno. retorna NULL en c++ (seguramente retorne nullptr en c++11 en adelante)
- strchr(c_str, chr) igual a strstr pero con char
- strrchr(c_str, chr) igual a strchr pero de derecha a izquierda
- strncpy(c_str destino, c_str src, n) copia n caracteres
- strncat(c_str, c_str, n) concatena n caracteres
- strncmp(c_str, c_str, n) copia n caracteres
- strlwr(c_str) -> TO_LOWER -> borland
- strupt(c_str) -> TO UPPER -> Borland
- strrev(c_str) -> invierte cadena -> Borland

#include <iomanip>
- setprecision()
- fixed
- setw()
- setfill()

Palabras reservadas/operaciones:

- caracteres de control. En especial '\n'
- if
- switch -> default
- for i
- i++ | i += 1 | i *= i
- while
- do while
- x % y = resto de x/y
- define
- typedef (typedef char str20[21];)
- static (en funciones)
- struct
- new
- delete